# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html

# Site
title: 靖翼之憩
subtitle: '挫其锐解其纷，和其光同其尘'
description: '靖翼子的个人博客 — 记录科研日常'
keywords:
author: 靖翼子
language: zh-CN
timezone: 'Asia/Shanghai'

# URL
url: https://jingyizi.github.io
root: /
permalink: :year/:month/:day/:title/
permalink_defaults:

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:
  - 'GUIDE.md'        # 维护指南

# Writing
new_post_name: :title.md
default_layout: post
titlecase: false
external_link:
  enable: true
  field: site
filename_case: 0
render_drafts: false
post_asset_folder: false
highlight:
  enable: true
  line_number: true
  auto_detect: false
  tab_replace: ''
prismjs:
  enable: false

# Home page setting (Fluid 主题使用 index 布局)
index_generator:
  per_page: 10
  order_by: -date

# Local search (for Fluid theme)
search:
  path: search.xml
  field: post
  content: true

# Category & Tag
default_category: uncategorized
category_map:
tag_map:

# Date / Time format
date_format: YYYY-MM-DD
time_format: HH:mm:ss

# Pagination
per_page: 10
pagination_dir: page

# Extensions
theme: fluid

# Deployment (handled by GitHub Actions — see .github/workflows/deploy.yml)
